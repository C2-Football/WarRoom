# Selective account release plan — Sep20

Status: reviewed candidate ready for selective deployment after completed migration rehearsal. The six named account endpoints were selectively deployed from `0a704ac`; fresh downloaded entrypoints/shared dependencies match exactly. Hosted behavioral checks continue. Independent review found a concurrent signup/OAuth rollback can delete a just-opened account; see `hosted-auth-independent-review.md`. Correction `f8ac223`, integrated `b429d71`, implements atomic account + initial subscription creation and passes the independent race/SQL regression. Current public signup remains unsafe for designated reusable QA addresses.

The intended bounded batch preserves current native/backend source `aa13193`, gift/trial/DHQ entitlement contracts, current sender/origins, and imports reviewed app-session, OAuth and rate protections. Five existing account endpoints are under review: `fw-signup`, `fw-signin`, `fw-profile`, `fw-oauth-sync`, `fw-refresh-session`. New `fw-change-password` and its already reviewed `20260918030000` migration require their own database preflight/apply evidence. Do not run the broad C2 backend workflow while AI, billing, diagnostics and provider differences remain unreconciled.

Before deployment: reproduce and fix the reviewed creation race; run actual production-handler and SQL rollback/concurrency tests; independently review; inspect current hosted schema and exact scoped migrations; rehearse any DDL with rollback safely. Save current function bundles/source metadata and hashes already inventoried. Commit the integrated candidate and migration plan. Deploy only named endpoints after compatible migrations, preserving current gateway settings. Download each new bundle into separate directories, compare entrypoint and shared dependency hashes to the reviewed revision, and check supported CORS/recovery routes. Roll forward to a reviewed compatible correction if needed; an older snapshot containing known deletion or session-revival defects is not an acceptable long-term rollback.

Hosted verification will use a **new internal operator-provisioned QA run**, random reserved-domain emails, known new UUIDs, PBKDF2 credentials, session version1 and exactly one free requested subscription each. Prepared plan and generated SQL remain in separate mode0600 private files. Read-only preflight proves no existing account, gift, role, subscription or game membership for either identity. Insertion must be one transaction with no upserts and rollback on any unexpected gift/role/collection. This does not invoke/relax the public reserved-email policy, and does not prove public signup, OAuth provider completion or email delivery. No original Sep18 identity or deleted room will be recreated.

After safe setup: sign in through the public password endpoint, verify per-account profiles and empty Vault/Duat collections, then run hosted password competition/old-session invalidation and isolated game journeys. Preserve original failure/cleanup evidence. No real customer league, record, payment, external provider credential or email will be touched.
