# Preserve current operational endpoints — 2026-09-20

Follow-up to atomic auth correction `f8ac223`; branch `codex/readiness-hosted-reconcile-20260920`. This batch imports the current `admin-list-users`, `admin-analytics-report` and `nfl-scoreboard` entrypoints exactly from their downloaded hosted sources, which match native revision `aa13193b28c552f977fa36a4683bb3a688d3da4f` byte for byte. It prevents a later C2 full-function deployment from replacing newer operational behavior with older implementations. No endpoint was deployed or user record read by this lane.

Preserved analytics behavior includes account/guest/door/error/sign-in detail, current production/native filtering, existing owner guest exclusion, account-to-Sleeper analytics bridging, and database rollups. Both admin entrypoints use the strengthened shared `resolveAppUserId` plus database role checks. Actual handler tests exercise ordinary member denial, revoked app tokens refusing OAuth fallback, unconfirmed email denial, confirmed OAuth administrator access and primary database failures. They also check production/native guest inclusion and sandbox/owner exclusion. Static helper-name assertions now refer to the composed helper; executable checks protect the underlying role/version boundary.

The scoreboard intentionally serves public GET-only NFL provider data with wildcard CORS and no credential-sharing flag. The actual handler test checks preflight, unsupported method and invalid season/week rejection, fixed provider URL, successful data despite cache-write failure, cache reuse and truthful upstream error without invented scores. This is preservation of the existing hosted contract, not a new permission grant to private data.

`tests/hosted-operations-reconciliation.cjs`, full `npm run test:security`, Deno checks of all three endpoints, and `git diff --check` pass. `npm run test:analytics` passes five active checks and reports one existing WIP for missing landing funnel instrumentation; that WIP is not counted as verified. This batch does not establish analytics completeness, administrative browser journeys, or frontend rollout.

A read-only schema query verified all referenced admin RPCs exist and are executable by service_role only: [grants and definition fingerprints](evidence/hosted-admin-rpc-preflight-20260920.json). The two newer helpers are absent from repository migration history; their exact deployed SELECT-only definitions are preserved as evidence, [account roster](evidence/hosted-admin_account_roster-20260920.sql) and [door surfaces](evidence/hosted-admin_doors_surfaces-20260920.sql). No SQL was applied. Existing-host compatibility is verified; rebuilding these diagnostics on a new database still needs migration-history reconciliation.

Independent native review ran the actual operations tests and inspected the entrypoint imports. Its prerequisite concern prompted the hosted RPC/grant verification above; it then inspected those definitions/grants, reran tests and reported no new material finding in this bounded restoration. Full backend deployment remains held for separate AI/billing/provider reconciliation. Both `ops-*` hosted endpoints were separately confirmed to be inert retired410 stubs, with no secret or database access.
